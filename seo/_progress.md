# SEO Implementation — Resume Ledger

Source spec: `HANDKIND_SEO_IMPLEMENTATION_v2.5_FINAL.md` (2026-08-12). Read `/seo/*.md` files below before resuming any session — they hold the durable state.

Phasing (§0.7): **Phase 1** evidence/baselines (read-only) → **Phase 2** technical foundation → **Phase 3** on-page (gated on T-04+T-05) → **Phase 4** expansion/authority/measurement.

| Task | Phase | Status | Commit | Notes/blocker |
|---|---|---|---|---|
| T-01 | 1 | done | — | Platform inventoried. **Critical finding:** live host is real Apache (132.148.183.84), NOT GitHub Pages despite `.github/workflows/static.yml` and `HOMEPAGE-REMEDIATION-SPEC.md` claiming GH Pages. `.htaccess` redirects (https-force, non-www-force, `/index.html`→`/`, `/blog/index.html`→`/blog/`) are committed but **not functioning live** — http/www/https-www all return 200 identically. User confirmed deploy is cPanel Git/FTP auto-deploy, separate from the GH Pages workflow. Root cause (AllowOverride off vs. `.htaccess` not syncing, e.g. FTP tools hiding dotfiles) unconfirmed — needs hosting-panel check. See `preflight-inventory.md`. |
| T-02 | 1 | done | — | Real inventory: ~44 indexable pages (not ~1,000). Full table in `preflight-inventory.md`. Flagged: `pages/locations/cambridge.html` exists but isn't one of the spec's 3 named location pages — prior commit shows a "Cambridge deindex" pass (2026-07-17); verify current indexability intent in Phase 2. |
| T-03 | 1 | done | — | Metadata snapshot of priority pages captured from repo source (not live-verified, per T-01 caveat). All pages have unique titles/descriptions and correct self-canonicals already. `preflight-inventory.md`. |
| T-04 | 1 | **in progress** | — | Adam pulled the standard GSC Performance export (2026-08-13) — confirmed it's the same non-joined Queries/Pages shape the spec already flagged (Queries.csv still only 50% of clicks). Template built at `query-page-map.md` with the 18 priority queries and exact per-query filter steps — Adam is filling it in via GSC UI. Still gates Phase 3 (T-12–T-25) until filled. Bonus finding: Coverage export shows 8 pages 404ing to Googlebot, only 30/68 known URLs indexed — see `coverage-404-investigation.md`, Adam pulling the specific URL list next. |
| T-05 | 1 | **done** | — | Adam confirmed 2026-08-13: in-code NAP is authoritative (HandKind Construction · 20 Balmoral St, Paris, ON N3L 0C8 · (226) 938-7108 · hello@handkindconstruction.ca). Note: this closed the address/phone/email question — postal code was actually wrong in schema (N3L 0A1) and has been corrected, see T-22. |
| T-36a | 1 | **done** | — | GA4 (`G-2Q3G09W17B`) already tracks `phone_click`, `email_click`, `generate_lead` (form submit via thank-you.html), and CTA/engagement events. See `tracking-audit.md`. Estimate-vs-contact form split fixed 2026-08-13 (`thank-you.html` now sets `form_id` from `document.referrer`). |
| T-35 | 1 | todo | — | GBP audit — Adam-facing (needs GBP dashboard access). |
| T-37 | 1 | blocked | — | BLOCKED — DATA ACCESS REQUIRED (Ahrefs/Semrush not connected). |
| T-38 | 1 | **done (provisional)** | — | Audited against known in-code NAP (not yet T-05-confirmed) — see `citation-audit.md`. **No NAP conflicts** across Yelp, YellowPages, Town of Paris directory, D&B, ZoomInfo, Yably — all consistent. Real finding is **coverage, not accuracy**: no claimed presence found on Bing Places, Apple Business Connect, HomeStars, or Houzz — all free and where tracked competitors do show up. Facebook NAP unverified (page didn't render via fetch). Re-verify once T-05 formally closes. |
| T-39 | 1 | blocked | — | BLOCKED — DATA ACCESS REQUIRED (same as T-37). |
| T-41 | 1 | **done (directional)** | — | The `handkind-seo-tracker` dashboard can't actually run in this Artifact publishing environment (needs direct Anthropic API fetch + `window.storage`, neither available — only `downloads`/`mcp` capabilities are granted here). Substituted WebSearch-based directional visibility research across the 8 tracked keywords — see `competitor-audit.md`. **Not verified numeric rank data** — no local pack visibility either. Key finding: HandKind shows up for its own long-tail Paris/Brantford/Brant-County combo terms but is absent from shorter high-volume commercial terms (matches spec's GSC evidence exactly). Hache Construction is the most consistently visible competitor. For real position tracking, either run the dashboard from within claude.ai directly, or connect a rank-tracking API. |
| T-42 | 1 | blocked | — | BLOCKED — DATA ACCESS REQUIRED. PageSpeed Insights public API quota is exhausted for the shared default project (no personal API key configured). Needs a Google API key (PageSpeed Insights API enabled) to proceed, or Adam can run PSI manually via the web UI and share results. |
| T-06 | 2 | **mostly done** | 8f4bc74 | Adam applied a fix directly on the live server (repo's `.htaccess` was untouched — confirms deploy doesn't reliably sync it). Live-verified 2026-08-13: http→https ✅ single hop. www→apex now fixed to single-hop in the repo (was a 2-hop chain: http/www→https/www→apex) — commit `8f4bc74`, pushed to PR #67, **not yet applied live** (needs the same manual-apply process Adam used before). |
| T-07 | 2 | **done** | — | `/blog` → `/blog/` and `/blog/index.html` → `/blog/` both live-verified working, single hop. |
| T-09 | 2 | todo | — | Parameterized URL (`rwg_token`) handling — not yet re-audited against the live fix; lower priority, revisit after T-06/T-08(index.html) close. |
| **T-06 open issue** | 2 | blocked | — | `/index.html` → `/` (repo rule 3, unchanged) still returns live `200` instead of redirecting, even though the other 3 rules now work. Adam is checking what's actually deployed on the server vs. the repo file — repo and production may still be out of sync beyond just this session's fix. |
| T-08 | 2 | **done** | — | Self-referential canonicals already correct site-wide per T-03 metadata snapshot. |
| T-10 | 2 | **done** | — | Sitemap already clean — all 45 real pages present, `cambridge.html` correctly excluded, no param/host variants. No edit needed. See `sitemap-robots-audit.md`. |
| T-11 | 2 | **done** | — | `robots.txt` already correct — `/pages/`, `/blog/`, `/css/`, `/js/` all crawlable, sitemap referenced. No edit needed. See `sitemap-robots-audit.md`. |
| T-22 | 3 | **done (partial)** | 1ae27b3 | T-05 unblocked this one (doesn't need query data). Fixed: wrong postal code (N3L 0A1→N3L 0C8) in `index.html` + `pages/contact.html`; removed self-serving `aggregateRating` block from `index.html` + `pages/reviews.html` (stale/inconsistent review count, violated Google's structured-data guidelines per prior remediation note). Not yet done: typed `City`/`AdministrativeArea` areaServed objects (spec's other T-22 ask) — low priority, can batch with T-04-gated work. |
| T-14 | 3 | **done** | — | Basement page: diagnosed via confirmed T-04 evidence — "basement renovation brantford" (316 impr) had zero impressions on the dedicated page because it exclusively used "basement finishing" terminology (36 mentions, 0 "renovation") in title/meta/H1/body/all 10 internal links. Fixed: title, meta description, H1, og/twitter tags, intro copy, and JSON-LD Service name now include "renovation" alongside "finishing". Verified locally (JSON-LD valid, no console errors, title/H1 render correctly). Left "finishing" as primary term and sitewide anchor text untouched — the target page's own content was the actual gap. |
| T-18 | 3 | **done** | — | Homepage service-card anchors were all generic "Learn more →" — replaced with descriptive text (Kitchen Renovations, Bathroom Renovations, Basement Finishing, Home Additions & ARUs), matching the spec's own examples. Confirmed nav already links to `/pages/services.html` (services hub) and location-page links were already descriptive ("Brantford →" etc.) — no further change needed there. Not gated by T-04 since it's structural/anchor-text, not content strategy. |
| T-21 | 3 | **done** | — | Audited all 10 project pages for a link back to their demonstrated service. 8/10 already had one; 1 (`project-kathleen-garage-paris.html`) is a correctly-configured `noindex` redirect stub to `project-detached-garage-paris.html`, not real content — excluded, no action needed. Fixed the 2 real gaps: `project-garage-overhaul-brantford.html` now links to the additions/ARU page (matching sibling garage project pages); `project-pharmacy-fitout-brantford.html` links to the general services hub rather than a residential service page, since no dedicated commercial-fitout page exists (per T-26, creating one isn't justified without evidence — didn't force a mismatched link). |
| T-19 | 3 | **done** | — | Checked all 5 blog→money-page routes the spec names. 4/5 already correct (egress-windows→basement, hst-rebate→additions, addition-cost→additions/ARU, all 5 kitchen articles→kitchen page). 1 real gap: the ARU guide had **zero** links to either target the spec calls for. Fixed: replaced a generic services.html link with the additions/ARU page, and added a basement-page link in the basement project-proof block ("many secondary-suite conversions build on" basement finishing). |
| T-20 | 3 | **done (verified, no changes needed)** | — | Already fully satisfied: all 4 service pages link to all 3 location pages; all 3 location pages link to all 4 core services. |
| T-23 | 3 | **done (verified, no changes needed)** | — | Every real content page has BreadcrumbList schema. Only exception is `project-kathleen-garage-paris.html`, a correctly-configured noindex redirect stub (no breadcrumb needed there). |
| T-24 | 3 | **done (verified, no changes needed)** | — | All 4 core service pages already have Service schema. |
| T-25 | 3 | **done (verified, no changes needed)** | — | FAQPage schema exists on 9 pages (pre-dates the May 2026 rich-result retirement). Spot-checked basement page: all 3 schema questions appear verbatim in visible on-page content — compliant, no hidden/mismatched Q&A. Per spec's neutral stance, no removal required and no further investment justified either. |
| T-12–T-13, T-15–T-17 | 3 | blocked | — | Still gated on T-04 (query ownership) — not started. Other queries confirmed so far (contractor near me, general contractor, general contractor brantford) all point to homepage already correctly owning those terms — no dedicated-page action needed for those. |
| T-26–T-27 | 4 | blocked | — | Depends on T-04. |
| T-28 | Required | **partial** | — | Repo-source pass done: 0 broken internal links, 0 JSON-LD parse errors across all 49 HTML files. See `crawl-health-baseline.md`. Duplicate-title/meta check, mobile/CLS, live redirect chains still deferred to after Phase 2/3. |
| T-29 | Required | blocked | — | Blocked with T-06/T-07/T-09 — validates redirect behavior that isn't implemented yet. |
| T-30 | Required | **done** | — | All 45 sitemap URLs return live 200 directly, zero redirects, zero duplicates. See `sitemap-robots-audit.md`. |
| T-31–T-34 | Required | todo | — | Mostly executable once earlier phases land. |
| T-36 | 4 | todo | — | Review acquisition — Adam-facing, can start anytime in parallel. |
| T-40 | 4 | blocked | — | Depends on T-39. |

## Open items needing Adam
1. **Hosting panel access** (or a direct answer): is `AllowOverride All` enabled for the handkindconstruction.ca vhost, and does the cPanel Git/FTP deploy actually transfer the `.htaccess` file (dotfiles are sometimes excluded by default in FTP clients/deploy configs)? This blocks T-06 with any confidence.
2. **GSC access** (T-04) — filtered query×page export or Search Console API credentials.
3. **GBP access or direct NAP confirmation** (T-05) — legal name, address, public phone, public email.
4. **Ahrefs/Semrush** (T-37, T-39) — if not available, those tasks stay blocked per spec (never fabricate).
